# Raw training data - fetched by the commands in README.md, not committed.
# bank-full.csv is 4.6 MB and freely available from UCI; only the held-out
# test_data.csv (needed by the deployed app) is tracked.
.data/

__pycache__/
*.py[cod]
.ipynb_checkpoints/
.DS_Store

# Editors / IDEs
.idea/
.jbeval
.vscode/

.venv/
venv/
.streamlit/secrets.toml

# Local tooling, not part of the assignment submission: the notebook generators
# and the script that assembles the submission PDF. Kept on disk, out of the repo.
/make_submission_pdf.py
/model/build_notebook.py
/model/build_lab_notebook.py
/model/bits_lab_standalone.py
/model/ML_Assignment2.ipynb
